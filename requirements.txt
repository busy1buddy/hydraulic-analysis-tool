# EPANET Hydraulic Analysis - Python Dependencies
# Install with: pip install -r requirements.txt

# Core hydraulic analysis
wntr>=1.4.0              # EPANET wrapper (steady-state, network modelling)
tsnet>=0.3.1             # Transient/water hammer analysis (MOC solver)

# Legacy web layer (reference only — PyQt6 desktop is the primary UI)
fastapi>=0.100.0         # legacy app/server.py
uvicorn>=0.20.0          # legacy ASGI server
nicegui>=2.0.0           # legacy app/main.py dashboard

# Testing
pytest>=7.0.0
httpx>=0.24.0
pytest-cov>=4.0.0

# Desktop application
PyQt6>=6.5.0             # Desktop UI framework
pyqtgraph>=0.13.0        # Interactive 2D plotting for network canvas

# Report generation
python-docx>=0.8.11      # DOCX report generation
fpdf2>=2.7.0             # PDF report generation

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
matplotlib>=3.7.0
networkx>=3.0
plotly>=5.15.0

# Review pipeline (optional)
anthropic>=0.40.0         # Automated review bridge (Anthropic API)
